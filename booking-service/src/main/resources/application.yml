spring:
  security:
    oauth2:
      client:
        registration:
          booking-service:
            provider: keycloak
            client-id: booking-service
            client-secret: 0ZsfkyWvYAkmpKltAz8LDTlDJtDF7CAj
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            issuer-uri: http://localhost:8180/realms/travel
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/travel
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/booking_service
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
management:
  server:
    port: 9002
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway,mappings
  endpoint:
    health:
      show-details: always
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
flight-service:
  base-url: http://localhost:8081
logging:
  file:
    name: logs/app.log
  level:
    root: INFO